name: Build rockchip target

on:
  push:
    paths:
      - config/rockchip/**
  workflow_dispatch:

jobs:
  build_ext:
    uses: ./.github/workflows/build.yml
    strategy:
      matrix:
        subtarget: [r2s,r4s]
    with:
      target: rockchip
      subtarget: ${{ matrix.subtarget }}